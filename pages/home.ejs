<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Coldoc</title>
		<link rel="stylesheet" href="/style.css" />
	</head>
	<body>
		<header class="home_header">
			<h1>Coldoc</h1>
			<p>Work on documents with your friends</p>
		</header>

		<main>
			<form action="/new" method="POST" class="home_form">
				<button class="button">Generate new document</button>
			</form>

			<% if (recent_documents?.length > 0) { %>
			<section class="recent_docs">
				<h2>Recent documents</h2>
				<ul>
					<% for (const doc of recent_documents) { %>
					<li>
						<a href="<%= doc.url %>" class="link">
							<%= doc.title %>
						</a>
					</li>
					<% } %>
				</ul>
			</section>
			<% } %>
		</main>
	</body>
</html>
